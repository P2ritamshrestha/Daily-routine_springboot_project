spring.application.name=login

# Server configuration
server.port=8080

# image configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
Profile.image=image/profilePicture


# Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/users_mgmt
spring.datasource.username=postgres
spring.datasource.password=p1ritam

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=guide.daily.pro@gmail.com
spring.mail.password=sret cddy lqxu foab
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.login.register=DEBUG

# Custom application properties
app.otp.expiration=300000  # OTP expiration time in milliseconds (5 minutes)